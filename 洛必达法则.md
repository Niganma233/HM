## 1. 适用对象：基本未定式
洛必达法则专门用于解决以下两种最基本的 [[未定式]] 极限：
1. **$\frac{0}{0}$ 型**：分子和分母的极限均为 $0$ 。
2. **$\frac{\infty}{\infty}$ 型**：分子和分母的极限均为 $\infty$（包括 $+\infty$ 和 $-\infty$）。
---
## 2. 法则陈述与适用条件（核心）
> [!note] 关于“定义域”与“值域”的说明
> 洛必达法则本身是一个定理，没有传统意义上的定义域和值域。但它的 **“适用条件”** 严格规定了函数 $f(x)$ 和 $g(x)$ 必须在怎样的局部范围内（即法则的“有效定义域”）满足特定性质，以及极限过程（自变量的趋近方式）。
### 2.1 当 $x \to x_0$ 时的法则（以 $\frac{0}{0}$ 型为例）
设函数 $f(x)$ 和 $g(x)$ 满足以下**四个严格条件**：
1. **局部极限条件**：$\lim_{x \to x_0} f(x) = 0$ 且 $\lim_{x \to x_0} g(x) = 0$。
2. **局部可导条件（法则的“定义域”）**：在点 $x_0$ 的某个 去心[[邻域]] $\mathring{U}(x_0, \delta)$ 内，$f(x)$ 和 $g(x)$ 均可导。
3. **分母导数非零条件**：在该去心邻域内，$g'(x) \neq 0$。
4. **导数极限存在条件（法则的“值域”趋向）**：极限 $\lim_{x \to x_0} \frac{f'(x)}{g'(x)}$ 存在（等于常数 $A$）或为 $\infty$。
**结论**：
在满足上述条件时，有：
$$ \lim_{x \to x_0} \frac{f(x)}{g(x)} = \lim_{x \to x_0} \frac{f'(x)}{g'(x)} = A \quad (\text{或 } \infty) $$
### 2.2 当 $x \to \infty$ 时的法则
条件完全类似，只需将“去心邻域”替换为“无穷远邻域”：
* **局部可导条件**：存在 $M > 0$，当 $|x| > M$ 时，$f(x)$ 和 $g(x)$ 均可导。
* **分母导数非零条件**：当 $|x| > M$ 时，$g'(x) \neq 0$。
* 其余条件（极限为未定式、导数极限存在）不变。
* **结论**：$\lim_{x \to \infty} \frac{f(x)}{g(x)} = \lim_{x \to \infty} \frac{f'(x)}{g'(x)}$
*(注：$\frac{\infty}{\infty}$ 型的法则陈述与上述完全一致，仅需将条件1改为分子分母极限均为 $\infty$。)*
---
## 3. 其他未定式的转化
除了 $\frac{0}{0}$ 和 $\frac{\infty}{\infty}$，还有五种其他类型的 `[[未定式]]`。洛必达法则不能直接用于它们，必须先通过代数变形转化为基本未定式

| 未定式类型             | 转化方法                         | 转化目标                                           |
| :---------------- | :--------------------------- | :--------------------------------------------- |
| $0 \cdot \infty$  | 将其中一个因子取倒数，移至分母              | $\frac{0}{0}$ 或 $\frac{\infty}{\infty}$        |
| $\infty - \infty$ | 通分、有理化或提取公因式                 | $\frac{0}{0}$ 或 $\frac{\infty}{\infty}$        |
| $0^0$             | 利用恒等式 $u^v = e^{v \ln u}$ 转化 | $\frac{0}{0}$ 或 $\frac{\infty}{\infty}$ (在指数上) |
| $1^\infty$        | 利用恒等式 $u^v = e^{v \ln u}$ 转化 | $\frac{0}{0}$ 或 $\frac{\infty}{\infty}$ (在指数上) |
| $\infty^0$        | 利用恒等式 $u^v = e^{v \ln u}$ 转化 | $\frac{0}{0}$ 或 $\frac{\infty}{\infty}$ (在指数上) |

> [!tip] 幂指函数转化核心
> 对于 $0^0, 1^\infty, \infty^0$ 型，核心思想是利用 [[指数函数]] 与 [[对数函数]] 的复合性质：
> $$ \lim f(x)^{g(x)} = \lim e^{g(x) \ln f(x)} = e^{\lim [g(x) \ln f(x)]} $$
> 从而将问题转化为求指数部分 $g(x) \ln f(x)$ 的 $0 \cdot \infty$ 型极限。
---
## 4. 洛必达法则的失效情况（避坑指南）
> [!danger] 重要警告
> 洛必达法则**不是万能的**。在使用时必须注意以下失效情况：
> 
1. **前提不满足**：如果分子分母的极限不是 $0$ 或 $\infty$（例如极限为 $\frac{1}{0}$），**绝对不能**使用洛必达法则。
2. **导数极限不存在（且不为 $\infty$）**：如果 $\lim \frac{f'(x)}{g'(x)}$ 不存在（例如出现振荡，像 $\sin \frac{1}{x}$ 那样），**不能**反推原极限 $\lim \frac{f(x)}{g(x)}$ 也不存在。此时洛必达法则失效，需改用 [[等价无穷小]] 替换或 [[泰勒公式]] 展开等其他方法。
3. **循环死胡同**：有时连续使用洛必达法则会导致式子越来越复杂，或者在两个式子之间无限循环。此时应停止使用，寻找其他代数化简方法。
---