
---
# 数列的极限
极限存在时称数列**收敛**，否则称**发散**。
## 定义
### 直观定义
当 $n$ 越来越大时，数列 $a_n$ 无限趋近于某个确定的常数 $A$，则称 $A$ 为数列 $\{a_n\}$ 的极限。
### 精确定义（$\varepsilon$-$N$ 语言）
设 $\{a_n\}$ 为一数列，$A$ 为常数。若对任意给定的正数 $\varepsilon > 0$，总存在正整数 $N$，使得当 $n > N$ 时，恒有
$$
|a_n - A| < \varepsilon
$$
则称数列 $\{a_n\}$ 收敛于 $A$，记作
$$
\lim_{n \to \infty} a_n = A
$$
或
$$
a_n \to A \quad (n \to \infty)
$$
## 收敛数列的性质
1. **唯一性**：若数列收敛，则其极限唯一。
2. **有界性**：收敛数列必有界，即有常数 $M > 0$，使 $|a_n| \le M$ 对所有 $n$ 成立。
3. **保号性**：若 $\lim a_n = A > 0$（或 $< 0$），则当 $n$ 充分大时，$a_n$ 与 $A$ 同号。
4. **保序性**：若 $a_n \le b_n$ 且两数列均收敛，则 $\lim a_n \le \lim b_n$。
## 重要定理
### 夹逼定理（迫敛性）
若存在 $N$，使得当 $n > N$ 时有
$$
b_n \le a_n \le c_n
$$
且
$$
\lim b_n = \lim c_n = A
$$
则
$$
\lim a_n = A
$$
### 单调有界定理
单调递增且有上界的数列必收敛；单调递减且有下界的数列必收敛。
## 常见极限类型
- 无穷小量：极限为 $0$ 的数列。
- 无穷大量：不收敛但绝对值无限增大的数列（注意：无穷大不属于极限存在）。
- 振荡数列：不收敛，也不趋于无穷（如 $(-1)^n$）。
> 注意：除法需保证 $b_n \neq 0$ 且 $B \neq 0$。
## 常用记号与术语
- $\lim_{n \to \infty} a_n = A$：读作“$a_n$ 的极限为 $A$”
- 收敛数列：$\exists A \in \mathbb{R}, \lim a_n = A$
- 发散数列：不收敛的数列
- 无穷小量：极限为 $0$ 的数列
- 无穷大量：$\forall M>0, \exists N, n>N \Rightarrow |a_n| > M$
## 注意事项
- $\varepsilon$-$N$ 定义中的 $N$ 不唯一，只要存在即可。
- 有界数列不一定收敛（如 $(-1)^n$）。

## PS
1. $$\lim_{n \to \infty} \left( 1 + \frac{1}{n} \right)^n = e$$
2. $$\lim_{x \to \infty} \frac{\sin x}{x} = 0$$