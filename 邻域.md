
> [!info] 核心概念
> 在 [[实数集]] $\mathbb{R}$ 中，**邻域**是用来描述一个点“附近”所有点的集合概念。它通过 [[绝对值]] 不等式来严格量化“距离”的远近，是后续定义[[极限]]和[[连续性]]的基石。
---
## 1. 点的邻域
设 $x_0$ 是实数轴上的任意一点，$\delta$ 是任意一个正实数（即 $\delta > 0$）。
* **定义**：满足不等式 $|x - x_0| < \delta$ 的所有实数 $x$ 的集合，称为点 $x_0$ 的 **$\delta$ 邻域**。
* **符号表示**：记作 $U(x_0, \delta)$。
* **集合表示**：
  $$ U(x_0, \delta) = \{x \mid |x - x_0| < \delta\} $$
* **区间表示**：
  $$ U(x_0, \delta) = (x_0 - \delta, x_0 + \delta) $$
* **几何意义**：在数轴上，它是以 $x_0$ 为中心，以 $\delta$ 为半径的**开区间**。点 $x_0$ 到该集合内任意一点 $x$ 的 [[距离]] 严格小于 $\delta$。
---
## 2. 去心邻域 (空心邻域)

> [!note] 为什么需要“去心”？
> 在研究 `[[极限]]` 时，我们只关心变量 $x$ 无限趋近于 $x_0$ 时函数的变化趋势，而**不关心**函数在 $x = x_0$ 这一确切点上的取值（甚至函数在该点可能无定义）。因此需要排除中心点本身。

* **定义**：点 $x_0$ 的 $\delta$ 邻域去掉中心点 $x_0$ 本身，称为点 $x_0$ 的 **$\delta$ 去心邻域**。
* **符号表示**：记作 $\mathring{U}(x_0, \delta)$ 或 $U^\circ(x_0, \delta)$。
* **集合表示**：
  $$ \mathring{U}(x_0, \delta) = \{x \mid 0 < |x - x_0| < \delta\} $$
* **区间表示**：
  $$ \mathring{U}(x_0, \delta) = (x_0 - \delta, x_0) \cup (x_0, x_0 + \delta) $$
---
## 3. 单侧邻域
有时我们只需要考虑点 $x_0$ 的某一侧（左侧或右侧）附近的点，这就引入了单侧邻域。
### 3.1 左邻域
* **定义**：点 $x_0$ 左侧的 $\delta$ 邻域。
* **符号表示**：$U_-(x_0, \delta)$
* **集合表示**：
  $$ U_-(x_0, \delta) = \{x \mid x_0 - \delta < x < x_0\} = (x_0 - \delta, x_0) $$
### 3.2 右邻域
* **定义**：点 $x_0$ 右侧的 $\delta$ 邻域。
* **符号表示**：$U_+(x_0, \delta)$
* **集合表示**：
  $$ U_+(x_0, \delta) = \{x \mid x_0 < x < x_0 + \delta\} = (x_0, x_0 + \delta) $$
*(注：同样可以定义“去心左邻域”和“去心右邻域”，只需在不等式中加上 $x \neq x_0$ 即可。)*
---